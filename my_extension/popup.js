document.getElementById('fetch-data').addEventListener('click', () => {
    // Перенаправление на страницу чата
    window.open('http://localhost:8000/chat/', '_blank'); // Открывает в новой вкладке
});
